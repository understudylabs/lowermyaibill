{
  "name": "lowermyaibill",
  "version": "0.1.0",
  "description": "Local-first Anthropic spend audit for Claude Code.",
  "type": "module",
  "bin": {
    "lmab": "bin/lmab"
  },
  "scripts": {
    "test": "node --test tests/*.test.mjs",
    "check": "node --check bin/lmab && node --check src/*.mjs && npm test"
  },
  "engines": {
    "node": ">=20"
  },
  "license": "MIT"
}

